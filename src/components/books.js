import React from 'react'

const Books = (props) => {
    return(
        <div>
           {/* {props.books.books.data.map(book => <li>book.attributes.title</li>)} */}
        </div>
    )
}

export default Books